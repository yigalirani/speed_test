testing speed of calling functions from array. used js,as ans cpp
the results: js ans as can call about 120 millions functions a second (yes, its the same speed)
cpp:500 millions a seconds (4 times as fast)

to run the js:
# npm install
# npm run asbuild
# npm run build
# on another cmd, open ht index and click the button on the page

to the js, use visual studio